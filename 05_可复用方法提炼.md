# 可复用方法提炼（修订版）

## 1. 通用诊断 Workflow

Trigger → Context Freeze → Conditional Compare → Evidence Gate → Ranked Hypotheses → Minimal Intervention → Expert Review → Learn

这套流程可用于训练异常、数据问题、实验指标分析和性能异常。迁移时只替换数据适配器、可比条件和验证动作，不复制具体根因文案或阈值。

## 2. 可复用的诊断笔记模板

诊断笔记是 Workflow 中“专家复核 → 知识沉淀”的标准组件，用于把一次排障经验转成可检索、可验证的知识。

| 字段 | 内容要求 |
|---|---|
| 异常表象 | 指标、时间窗、step、影响范围 |
| 排查路径 | 检查顺序、使用的数据源、排除项 |
| 最终根因 | 经验证的原因，标明观测证据或干预证据 |
| 修复动作 | 唯一变更、回退方式、验证结果 |
| 适用条件 | 模型、阶段、数据口径、版本等适用范围 |
| 排除条件 | 已验证不成立或不可比的情况 |
| 证据引用 | 实验 ID、日志片段、配置 Diff、验证任务 ID |

## 3. 知识沉淀闭环

1. 系统根据本次诊断证据预填笔记。
2. 工程师校正异常表象、排查路径、根因和修复动作。
3. 必填字段执行 trim 校验，空白内容不得提交。
4. 专家确认后，系统将笔记结构化写入案例库。
5. 自动抽取“异常 → 根因 → 修复”关系，更新相似案例关联。
6. 工程师拒绝或补充根因时，原候选保留为负样本，防止错误经验污染知识库。

该闭环本身就是可复用能力，不依赖某一种异常类型；变化的只是字段解释、证据来源和验证方式。

## 4. 跨场景迁移示例

| 场景 | 主要证据 | 候选根因 | 最小验证动作 |
|---|---|---|---|
| 数据问题排查 | 分布、血缘、样本批次、标注版本 | 采样偏移、脏样本、标注回退 | 分层抽样复检一个数据版本 |
| 实验指标分析 | 同评测集、同口径、同时间窗 | 指标口径变化、配置变量、评测泄漏 | 只消融一个变量后重算 |
| 性能异常 | 拓扑、节点负载、通信事件、版本 | 热点节点、通信退化、版本回归 | 隔离单节点或单版本压测 |
| 流水线延迟 | DAG、依赖、队列、重试、输入规模 | 上游慢任务、重试风暴、分区倾斜 | 回放单分区并限制并发 |

## 5. 复用约束

- 各场景独立维护阈值、案例库和准入标准，避免知识串库。
- 保留“观测证据 / 干预证据”标签，不把相关性改写为因果。
- 只允许专家确认内容进入正式知识库。
- 先只读回放，再影子运行，最后开放隔离验证；高风险自动执行另立项目。
- 统一评估引用正确率、Top-3 召回、主动调查时间、错误恢复成本和人工打断次数。

## 6. 示例模板

{
  "name": "Loss Spike Custom v1",
  "trigger": "loss_zscore > 3",
  "steps": [
    "snapshot",
    "stage_align",
    "similarity_retrieval",
    "evidence_gate",
    "rank_hypotheses"
  ],
  "mode": "read_only"
}

Workflow 只描述诊断步骤，不授予修改线上任务的权限；每次执行仍需经过证据门和人工确认。