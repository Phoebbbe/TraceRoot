# TraceRoot 训练异常诊断：产品定位说明

## 一句话定位

TraceRoot 是嵌入训练平台的**证据驱动诊断助手**：在异常出现后，自动把指标、日志、配置、训练阶段和历史实验对齐，给出可反驳、可验证、可追溯的候选原因；它不替工程师裁决根因，也不自动修改线上训练任务。

## 要解决的问题

算法工程师能看到告警，却要在多个数据源之间手工翻查、找相似实验、比配置差异。相同异常的定位时间和质量高度依赖个人经验，结论又难以沉淀复用。最小目标是缩短“发现异常 → 得到可验证方向”的时间，并让每条判断都能回到原始证据。

## 现有方式与缺口

| 方式 | 已解决 | 关键缺口 |
|---|---|---|
| 人工翻日志/看板 | 能发现并处理长尾问题 | 反复切换数据源，依赖个人经验，难复用 |
| 指标告警 | 快速发现 loss、grad_norm 等异常 | 只描述症状，不做条件化历史对照 |
| 通用 LLM 总结 | 能快速归纳长日志 | 无权限内引用、反证和阶段约束时，结论不可采纳 |

TraceRoot 的价值不是再做一个聊天框，而是把检索、对齐、差异、证据门和最小验证组织成一个闭环。

## 核心闭环（本轮只做一件事）

以 `EXP-1042` 在 step 18,420 后的 loss 突增为固定场景：

1. 冻结异常窗口中的指标、日志、配置和数据阶段。
2. 只检索同模型、同 checkpoint、同数据阶段的可比实验。
3. 展示配置、数据阶段、代码、硬件四类 Diff，并保留冲突证据。
4. 输出 Top-3 候选，而非单一“确定根因”。
5. 对候选“数据阶段切换 + 未开启 `grad_clip`”生成单变量验证沙箱。
6. 工程师确认后，系统预填诊断笔记，连同适用/排除条件沉淀到案例库。

## 人与系统的边界

- 系统连续完成：指标对齐、日志摘录、历史召回、Diff 计算、引用校验、候选排序。
- 人只在高影响节点判断：是否启动深度诊断、是否消耗资源运行最小验证、是否确认并沉淀案例。
- 所有建议默认为只读；验证在隔离分支运行，禁止自动回滚、自动改线上配置。

## 成立与否的判定

### 必须同时满足的硬门槛

1. 每条引用可打开、来自当前用户有权限的数据源，虚构引用为 0。
2. 高可信结论能区分“观测证据”和“干预证据”，并展示反证与缺失信息。
3. 条件不匹配（模型、阶段、数据口径、代码版本）时自动降级为“不可比”，不能直接复用结论。
4. 错误结论可驳回，原始证据保留；知识库只接收专家确认内容。
5. 诊断失败不影响原训练任务，验证资源有上限、可取消、可回收。

### 可量化验收指标

| 指标 | 定义与测量 | 目标 |
|---|---|---|
| 引用正确率 | 可达且支持对应推断的引用数 / 引用总数；链接检查 + 专家盲审 | ≥99%，虚构引用 0 |
| Top-3 根因召回 | 标注集真实根因进入前 3 候选的比例 | ≥80% |
| 高可信精准率 | 经验证成立的高可信结论 / 全部高可信结论 | ≥90% |
| 主动调查时间 | 用户查看、切换、批注的活跃时间；与人工流程 A/B 对照 | P50 下降 ≥30% |
| 错误恢复成本 | 驳回到新有效假设的主动时间 + 浪费 GPU 时 | P50 ≤5 分钟；自动浪费 GPU=0 |
| 人工中断次数 | 一次标准诊断的 `decision_required` 事件数 | ≤3 次 |

任一安全、引用或精准率硬门槛失败，能力只能停留在历史回放/影子运行，不得进入真实训练推荐。

## 范围取舍

本轮交付证明“结论是否值得采纳”，不同时建设完整 SCM 因果引擎、事前预警、自动回滚或通用拖拽编排器。先把一个异常类型做透，再扩展异常覆盖面；这是控制误导风险和交付复杂度的必要取舍。

