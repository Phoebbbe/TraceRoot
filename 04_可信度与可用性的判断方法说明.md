# 可信度与可用性的判断方法

## 结论分级

| 等级 | 必要条件 | 产品表现 | 可执行范围 |
|---|---|---|---|
| 高 | ≥2 个独立来源 + 条件完全对齐 + 无未解释反证 + 有干预证据 | 显示“已验证”及引用 | 可进入只读推荐；仍需专家确认 |
| 中 | 多来源观测一致，但缺少干预或存在轻微缺口 | 显示“待验证”及缺口 | 只能创建隔离验证建议 |
| 低 | 单一来源、条件不可比、引用不可达或存在冲突 | 显示“证据不足” | 不输出根因结论，不入库 |

系统不得把模型生成的概率当作真实概率；概率仅是候选排序分数，必须同时展示证据构成。

## 证据质量检查

1. **来源**：引用指向权限内的指标、日志、配置快照或历史案例，记录实验 ID、时间窗、step、rank。
2. **对齐**：模型版本、checkpoint、数据阶段、数据口径、代码 commit、并行规模均可比；缺一项就降级。
3. **独立性**：至少一个指标/日志证据与一个历史或配置证据；同一复制链不能算两条独立证据。
4. **反证**：显示与候选相冲突的信号、缺失字段和未覆盖条件。
5. **可复现**：能从异常前 checkpoint 建立隔离分支，并记录随机种子、镜像、配置 diff。

## 离线与在线验证

### 离线历史回放

- 建立按模型、阶段、异常类型分层的标注集（≥200 个案例）。
- 回放真实输入，测 Top-3 召回、高可信精准率、引用正确率、不可比误报率。
- 由至少两名算法工程师独立标注，第三人仲裁；报告分层置信区间，不只给平均值。

### 影子运行

- 只读接入真实训练任务，不向用户推荐修复，不消耗额外 GPU。
- 记录触发延迟、上下文完整率、引用可达率、候选稳定性和人工复核结果。
- 连续两周无权限泄露、无虚构引用、无原任务影响，才进入小范围试点。

### 试点准入

满足以下全部条件后，才允许在真实训练场景提供“待验证”建议：

- 引用正确率 ≥99%，虚构引用为 0。
- Top-3 召回 ≥80%，高可信精准率 ≥90%。
- 上下文完整率 ≥95%，不可比案例不会被标成高可信。
- 标准路径人工打断 ≤3 次，P50 主动调查时间下降 ≥30%。
- 验证沙箱具备 GPU/时间上限、取消和自动清理；原任务影响为 0。
- 审计日志可查询，个人权限、团队权限和敏感字段经过脱敏策略验证。

任一红线失败即回退到影子运行。生产初期默认只读、默认“待验证”，由专家确认后才允许案例入库。

## 持续监控

每周抽样复核已采纳案例；监控数据分布漂移、模型/代码版本变化、引用失效率、被驳回率和验证成功率。指标连续两周期恶化时暂停推荐，保留数据供复盘。

